diff --git a/src/generate_documentation.py b/src/generate_documentation.py
index 1111111..2222222 100644
--- a/src/generate_documentation.py
+++ b/src/generate_documentation.py
@@ -1,5 +1,6 @@
 """GitHub Pull/Merge Request documentation generator."""
 
+from __future__ import annotations
 import argparse
 import json
@@ -120,6 +121,18 @@ def render_mode_b(files: Iterable[FileChange]) -> str:
     return "\n".join(lines).rstrip() + "\n"
 
 
+def extract_diff_details(git_diff: str) -> dict[str, list[str]]:
+    details: dict[str, list[str]] = {}
+    current_path: str | None = None
+    in_hunk = False
+    return details
+
+
 def render_mode_a(metadata: ContextMetadata, files: list[FileChange], git_diff: str) -> str:
     added = sum(1 for f in files if f.status == "added")
diff --git a/README.md b/README.md
index 3333333..4444444 100644
--- a/README.md
+++ b/README.md
@@ -10,3 +10,5 @@ python src/generate_documentation.py --context samples/context.initial.json
 ## Usage
 Run the generator with a JSON context file containing branch metadata and git diff data.
+
+Supports MODE A (standard merge) and MODE B (empty target branch / first commit).
